import 'package:flutter/material.dart';
class data {
  String title, image, subtitle,imageicon;
  data({
    required this.title,
    required this.image,
    required this.subtitle,
    required this.imageicon,

  });
}
